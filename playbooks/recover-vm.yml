---
- name: Recover VM image and inject SSH key (run on libvirt host)
  hosts: localhost
  connection: local
  become: yes
  vars:
    vm_name: "{{ vm_name | default('aap') }}"
  roles:
    - role: minirhis-recovery
