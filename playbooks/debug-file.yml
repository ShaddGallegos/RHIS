---

- name: Debug simple file task
  hosts: satellite
  become: true
  tasks:
    - name: Ensure /tmp/mrhis_debug exists
      ansible.builtin.file:
        path: /tmp/mrhis_debug
        state: directory
        owner: root
        group: root
        mode: '0755'
