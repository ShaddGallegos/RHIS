# Generated by MiniRHIS — managed block. Do not hand-edit this block.
# Reminder: The internal MINIRHIS network defaults to 10.168.0.0/16. You can
# change these values during installer prompts or in ~/.ansible/conf/env.yml
# (the installer persists answers there). Ensure the installer node has an
# entry on the internal network (gateway/DNS at INSTALLER_IP).
#
# Variables populated from ~/.ansible/conf/env.yml (if present):
#   SAT_IP, AAP_IP, IDM_IP, INSTALLER_IP, INTERNAL_NETWORK

{{ SAT_IP | default('10.168.128.1') }} satellite.prod.spg satellite
{{ AAP_IP | default('10.168.128.2') }} aap.prod.spg aap
{{ IDM_IP | default('10.168.128.3') }} idm.prod.spg idm
{{ INSTALLER_IP | default((INTERNAL_GW | default('10.168.0.1'))) }} installer.prod.spg installer

# End of generated block
