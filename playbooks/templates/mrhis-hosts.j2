# BEGIN RHIS INTERNAL HOSTS
# Generated by MRHIS — managed block. Do not hand-edit this block.
# Reminder: The internal MRHIS network defaults to 10.168.0.0/16. You can
# change these values during installer prompts or in ~/.ansible/conf/env.yml
# (the installer persists answers there). Ensure the installer node has an
# entry on the internal network (gateway/DNS at INSTALLER_IP).
#
# Variables populated from ~/.ansible/conf/env.yml (if present):
# SAT_IP, AAP_IP, IDM_IP, INSTALLER_IP, INTERNAL_NETWORK
# BEGIN MRHIS INTERNAL HOSTS

{{ SAT_IP | default('10.168.128.1') }} {{ SAT_HOSTNAME | default('satellite.' ~ (DOMAIN | default('prod.spg'))) }} satellite
{{ AAP_IP | default('10.168.128.2') }} {{ AAP_HOSTNAME | default('aap.' ~ (DOMAIN | default('prod.spg'))) }} aap
{{ IDM_IP | default('10.168.128.3') }} {{ IDM_HOSTNAME | default('idm.' ~ (DOMAIN | default('prod.spg'))) }} idm
{{ GITTEA_IP | default('10.168.128.4') }} {{ GITTEA_HOSTNAME | default('gittea.' ~ (DOMAIN | default('prod.spg'))) }} gittea
{{ OPENSHIFT_IP | default('10.168.128.5') }} {{ OPENSHIFT_HOSTNAME | default('openshift.' ~ (DOMAIN | default('prod.spg'))) }} openshift
{{ INSTALLER_IP | default((HOST_INT_IP | default('10.168.0.1'))) }} {{ INSTALLER_HOSTNAME | default('installer.' ~ (DOMAIN | default('prod.spg'))) }} installer

# END MRHIS INTERNAL HOSTS
# END RHIS INTERNAL HOSTS
