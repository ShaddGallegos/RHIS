# Custom ansible-lint rules for MRHIS

This directory is reserved for any custom ansible-lint rules. Currently CI enforces FQCN via scripts/ansible_fqcn_check.py.

## Headless Test & Vault

See the top-level README 'Headless Noninteractive Test (developer)' for the noninteractive test command and vault guidance.
